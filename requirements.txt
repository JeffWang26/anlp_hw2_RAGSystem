# ANLP HW2 RAG System - Core dependencies

# Document processing
beautifulsoup4>=4.12.0
requests>=2.28.0
pypdf>=4.0.0
pdfplumber>=0.10.0  # Optional: better PDF extraction for tables

# Embeddings & dense retrieval
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
# faiss-gpu  # Use for GPU (Colab/Linux with CUDA): pip install faiss-gpu

# Sparse retrieval (choose one)
rank-bm25>=0.2.2
# bm25s  # Alternative BM25 implementation

# NLP / ML
numpy>=1.24.0
scikit-learn>=1.3.0

# Document reader (QA / LLM)
transformers>=4.36.0
torch>=2.0.0
accelerate>=0.25.0
bitsandbytes>=0.46.1  # For 4-bit quantization on GPU (Colab): !pip install -U bitsandbytes accelerate
